{% extends 'chanppEvImBundle::layout.html.twig' %}

{% block body -%}
    <h1>Subir Archivo a Evaluación Indirecta</h1>

    <form action="{{ path('uploadindirecta' , { 'evaluacionindirecta_id': app.request.get('evaluacionindirecta_id')}) }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Subir Archivo</button>
        </p>
    </form>
{% endblock %}