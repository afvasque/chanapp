{% extends 'chanppEvImBundle::layout.html.twig' %}

{% block body -%}
    {% if entity.getEvaluacion().getDone() == false %}
    <h1>Editar Sub-Evaluación Indirecta</h1>
     <a href="{{ path('evaluacionindirecta_show', { 'id': entity.id}) }}" class="btn">
            Volver a Sub-Evaluación Indirecta
        </a>
        <a href="{{ path('metodorecoleccion' , { 'original_redirect': 2 , 'original_id': entity.id}) }}" class="btn btn-primary">
        Ver y Agregar Métodos de Recolección de Datos
    </a><br><br>
        <form action="{{ path('evaluacionindirecta_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit" onclick="return confirm('¿Seguro que desea eliminar?')" class="btn btn-danger">Eliminar</button>
        </form>
    <form action="{{ path('evaluacionindirecta_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        <input type="hidden" name="_method" value="PUT" />
        {{ form_widget(edit_form) }}
        <p>
            <button type="submit" class="btn btn-success">Guardar Cambios</button>
        </p>
    </form>
{% else %}
<h1>Error, la evaluación padre ({{entity.getEvaluacion.getNombre()}}) ha sido cerrada y sus sub-evaluaciones no pueden ser editadas.</h1>
{% endif %}
{% endblock %}
