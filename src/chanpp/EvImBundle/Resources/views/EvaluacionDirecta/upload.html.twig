{% extends 'chanppEvImBundle::layout.html.twig' %}

{% block body -%}
    <h1>Subir Archivo a Evaluación Directa</h1>
<div id="hidestupiderrors">
    <form action="{{ path('uploaddirecta' , { 'evaluaciondirecta_id': app.request.get('evaluaciondirecta_id')}) }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit" class="btn btn-success">Subir Archivo</button>
        </p>
    </form></div>
{% endblock %}