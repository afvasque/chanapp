{% extends 'chanppEvImBundle::layout.html.twig' %}

{% block body -%}
    <h1>Creación de Evaluación Directa</h1>
    <form action="{{ path('evaluaciondirecta_create', { 'evaluacion_id':app.request.get('evaluacion_id') }) }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Crear</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('planevaluacion_show', { 'id': app.request.get('evaluacion_id')}) }}">
            Volver a evaluación
        </a>
    </li>
</ul>
{% endblock %}
