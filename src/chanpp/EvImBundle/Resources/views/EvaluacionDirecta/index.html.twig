{% extends '::base.html.twig' %}

{% block body -%}
    <h1>EvaluacionDirecta list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Resultado</th>
                <th>Unidad</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('evaluaciondirecta_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.resultado }}</td>
                <td>{{ entity.unidad }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('evaluaciondirecta_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('evaluaciondirecta_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('evaluaciondirecta_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
