{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Meta</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ entity.descripcion }}</td>
            </tr>
            <tr>
                <th>Plazomes</th>
                <td>{{ entity.plazoMes }}</td>
            </tr>
            <tr>
                <th>Plazoanio</th>
                <td>{{ entity.plazoAnio }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('meta') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('meta_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('meta_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
