{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Meta list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Descripcion</th>
                <th>Plazomes</th>
                <th>Plazoanio</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('meta_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.descripcion }}</td>
                <td>{{ entity.plazoMes }}</td>
                <td>{{ entity.plazoAnio }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('meta_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('meta_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('meta_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
