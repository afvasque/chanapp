{% extends 'chanppEvImBundle::layout.html.twig' %}

{% block body -%}
    <h1>Editar Pregunta de Desarrollo</h1>
    <a href="{{ path('cuestionario_show' , {'id' : entity.getCuestionario().getId() }) }}" class="btn">
            Volver a cuestionario
        </a>
    <form action="{{ path('preguntadesarrollo_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit" class="btn btn-danger" onclick="return confirm('¿Seguro que desea eliminar?')">Eliminar</button>
        </form>
    <form action="{{ path('preguntadesarrollo_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        <input type="hidden" name="_method" value="PUT" />
        {{ form_widget(edit_form) }}
        <p>
            <button type="submit" class="btn btn-success" >Guardar Cambios</button>
        </p>
    </form>
{% endblock %}
