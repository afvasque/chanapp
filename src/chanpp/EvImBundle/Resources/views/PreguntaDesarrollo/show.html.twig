{% extends '::base.html.twig' %}

{% block body -%}
    <h1>PreguntaDesarrollo</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Enunciado</th>
                <td>{{ entity.enunciado }}</td>
            </tr>
            <tr>
                <th>Eje</th>
                <td>{{ entity.eje }}</td>
            </tr>
            <tr>
                <th>Numeropregunta</th>
                <td>{{ entity.numeropregunta }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('preguntadesarrollo') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('preguntadesarrollo_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('preguntadesarrollo_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
